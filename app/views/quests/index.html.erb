<h1>Listing Quests</h1>

<table class="pure-table pure-table-horizontal">
  <thead>
    <tr>
      <th>Quest</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @quests.each do |quest| %>
      <tr>
        <td><%= link_to quest.title, quest %></td>
        <td><%= link_to ' ', edit_quest_path(quest), :class => "fa fa-pencil-square-o" %></td>
        <td><%= link_to ' ', quest, method: :delete, data: { confirm: 'Are you sure?' }, :class => "fa fa-trash-o" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Quest', new_quest_path, :role => 'button', :class => "notice"  %>
